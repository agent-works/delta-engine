name: agent-with-circular-imports
version: 1.9.0
description: Test agent with circular import (should fail)

imports:
  - module-a.yaml

llm:
  model: gpt-4-turbo-preview
  temperature: 0.7
  max_tokens: 1000

tools:
  - name: local_tool
    description: Local tool
    exec: "echo ${message}"

max_iterations: 30
